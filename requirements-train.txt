# WS-A LoRA fine-tune stack (heavy; GPU box only — NOT needed on the control plane).
# Install on the Ray worker image: pip install -r requirements.txt -r requirements-train.txt
#   atlas/ray_train_lora.py + slate/trainers/causal_lm_lora.py import these LAZILY, so the daemon and unit
#   tests run without them; only a real fine-tune needs them.
ray[train]>=2.9.0
torch>=2.2.0
transformers>=4.44.0
peft>=0.11.0
trl>=0.9.0
datasets>=2.19.0
accelerate>=0.31.0
bitsandbytes>=0.43.0  # 4-bit QLoRA; optional — trainer degrades to full-precision LoRA if absent
