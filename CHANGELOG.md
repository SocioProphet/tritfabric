# Changelog

## 0.1.0 (2026-01-14)

Fresh consolidated repository generated from the TritFabricAI working spec.

Includes:
- Atlas OS service scaffold (orchestrator + registry + DRF scheduler + router/autoscaler stubs)
- Promotion gates scaffold (SHACL + ONNX cosine + eval deltas)
- Proto surface (`api/atlas/v1/atlas.proto`) + buf scaffolding
- GitOps skeletons (ArgoCD AppSet, Rollouts, Gatekeeper, Image Updater, ExternalSecrets)
- Dockerfiles + Helm chart skeletons
- Integration snapshots for the provided related repos under `integrations/`
