"""Teacher model export scaffold.

This is intentionally a placeholder. In a full implementation, this script would:
- load a pretrained teacher model
- run inference to produce logits on a dataset shard
- write teacher logits to an artifact store for distillation

Used by: `slate/distill/kd_loss.py`
"""
