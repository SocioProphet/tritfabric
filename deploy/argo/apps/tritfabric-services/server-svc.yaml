apiVersion: v1
kind: Service
metadata:
  name: tritfabric-server
spec:
  selector:
    app.kubernetes.io/name: tritfabric-server
  ports:
    - name: http
      port: 80
      targetPort: 8000
    - name: metrics
      port: 9108
      targetPort: 9108
