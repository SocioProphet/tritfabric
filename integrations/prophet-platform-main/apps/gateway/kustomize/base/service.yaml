apiVersion: v1
kind: Service
metadata:
  name: socioprophet-gateway
spec:
  selector: { app: socioprophet-gateway }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
