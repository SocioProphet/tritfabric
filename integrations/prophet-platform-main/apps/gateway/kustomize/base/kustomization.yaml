apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: gateway-config
    literals:
      - TRITRPC_SOCK=/tmp/socioprophet.sock
      - GATEWAY_PORT=8080
