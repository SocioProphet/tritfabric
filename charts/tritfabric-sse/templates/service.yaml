apiVersion: v1
kind: Service
metadata:
  name: {{ include "tritfabric-sse.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "tritfabric-sse.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
